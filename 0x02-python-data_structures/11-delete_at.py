#!/usr/bin/python3
def delete_at(my_list=[], idx=0):
    '''coding with py is fun'''
    if idx < 0:
        return (my_list)
    '''if a list is found delete it!'''
    if idx < len(my_list);
        del(my_list[idx])
    return (my_list)
